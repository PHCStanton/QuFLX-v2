# Icons Updated

I have updated the icons for **Immediate Entries** (`EntryIcon`), **Indicators** (`IndicatorsIcon`), and **Top-Down** (`TopDownIcon`) to use a clean **line-only design** (no fills, only strokes).

- **Entries**: An outline circle with a checkmark.
- **Indicators**: A smooth curve line chart.
- **Top-Down**: A central arrow passing through horizontal levels.

These should now integrate seamlessly with the neomorphic look without heavy filled areas.
